spring:
  datasource:
    url: jdbc:h2:mem:blog-application
    username: sa
    password: password
    driver-class-name: org.h2.Driver
  jpa:
    open-in-view: false
springdoc:
  swagger-ui:
    url: /api.yml
  api-docs:
    enabled: true
